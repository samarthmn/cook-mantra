# Next.js loads browser-facing environment files from apps/web.
# Copy this file to apps/web/.env.local when the API is not using the default URL.
NEXT_PUBLIC_COOK_MANTRA_API_URL=http://127.0.0.1:8000/api/v1
